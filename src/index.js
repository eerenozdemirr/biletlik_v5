import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Genel stil dosyası
import App from './App'; // Uygulama bileşeni

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
